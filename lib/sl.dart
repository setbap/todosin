import 'package:get_it/get_it.dart';

var locator = GetIt.I;

void setupLocator() {
  // locator.registerSingleton(DbManager());
  // locator.registerFactory(() => ResualtClass(locator()));
  print("serice locator");
}
